name: Digest definition diagnostic

on:
  workflow_dispatch:

jobs:
  digest:
    runs-on: ubuntu-latest
    steps:
      - name: Test configured secrets
        run: echo configured
        env:
          AI_API_KEY: ${{ secrets.AI_API_KEY }}
          FEISHU_WEBHOOK_URL: ${{ secrets.FEISHU_WEBHOOK_URL }}