name: Digest definition diagnostic

on:
  workflow_dispatch:

jobs:
  digest:
    runs-on: ubuntu-latest
    steps:
      - name: Test DeepSeek secret only
        run: echo configured
        env:
          AI_API_KEY: ${{ secrets.AI_API_KEY }}